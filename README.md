# Django Work Project

Учебный проект: инициализация Django с двумя приложениями.

## Приложения
- `workplaces` — рабочие места
- `employees` — сотрудники

## Что сделано
- Создан Django-проект
- Созданы и зарегистрированы приложения в `INSTALLED_APPS`
- Сохранены зависимости в `requirements.txt`

## Установка
```bash
python -m venv venv
# Windows: venv\Scripts\activate
# macOS/Linux: source venv/bin/activate
pip install -r requirements.txt


## Задание: Django — Сотрудники и рабочие места

Второе задание по Django: я сделала модели и настроила админку.

### Что я сделала:

- Добавила профиль сотрудника: отчество, пол, описание. Профиль привязан к обычному пользователю Django.
- Сделала навыки и уровень владения (от 1 до 10). У одного сотрудника может быть много навыков, и для каждого — свой уровень.
- Добавила рабочее место и связала его с сотрудником: у одного стола — один сотрудник.
- Настроила админку: все модели там видны, а навыки можно добавлять прямо в карточке сотрудника.

### Где какой код лежит

- `employees/models.py` — тут модели про сотрудников и навыки.
- `workplaces/models.py` — тут модель про рабочее место.
- `admin.py` в обеих папках — настройка админки.
